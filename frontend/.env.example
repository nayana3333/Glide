# Copy to .env and adjust for your environment.
# Left unset, the app talks to http://localhost:8000 (the local dev backend).
# In production (e.g. Vercel project settings), set this to the deployed
# backend's URL, e.g. https://glide-backend.onrender.com
VITE_API_URL=
